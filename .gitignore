__pycache__/
*.pyc
*.pyo
.env
*.egg-info/
dist/
build/

# Large binary files (regenerable)
chunked/embeddings.npz
chunked/embeddings_metadata.json
chunked/centroids.npz
chunked/passages.jsonl
chunked/cleaned_passages.jsonl

# Backup files
*.bak

# Bulk export (Phase 6) — regenerated by scripts/build_export.py, hosted
# on Cloudflare R2, not committed to the repo.
export/
.wrangler/
